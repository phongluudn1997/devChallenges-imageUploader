import React from "react";

interface PropsType {
  file: File;
  url: string;
}

const ImageReview = (props: any) => {
  return (
    <div>
      <h3>Uploaded Successfully!</h3>
    </div>
  );
};

export default ImageReview;
